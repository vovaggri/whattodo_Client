// MARK: - Module Definition

enum CreateGTask {
    enum Fetch {
        struct Request { }
        struct Response {
            let defaultDescription: String
        }
        struct ViewModel {
            let defaultDescription: String
        }
    }
}
