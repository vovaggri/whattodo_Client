import UIKit

enum CreateGoal {
    enum Fetch {
        struct Request { }
        struct Response {
            let defaultDescription: String
        }
        struct ViewModel {
            let defaultDescription: String
        }
    }
}
