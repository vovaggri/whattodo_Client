import UIKit

enum SuccessScreen {
    enum SuccessMessage {
        struct Request {}
        struct Response {
            let message: String
        }
        struct ViewModel {
            let message: String
        }
    }
}
