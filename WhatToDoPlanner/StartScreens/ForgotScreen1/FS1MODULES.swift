enum ForgotScreen {
    enum Fetch {
        struct Request { }
        struct Response {
            let emailPlaceholder: String
        }
        struct ViewModel {
            let emailPlaceholder: String
        }
    }
}
